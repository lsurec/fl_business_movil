import 'package:flutter/material.dart';

class CategoryRestaurantPosView extends StatelessWidget {
  const CategoryRestaurantPosView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CategoryRestaurantPosView'),
      ),
    );
  }
}
